library(gammaFrailty)
varList <- sampleVar(
THETA = Opt0$theta,
DATA = dt,
X = x,
NU = 1,
METHOD = 1,
RANGE = Opt0$control$MAXT - Opt0$control$BURN,
TOTFLAG = TRUE,
PRINTFLAG = F
)
varList
varList <- sampleVar(
THETA = Opt0$theta,
DATA = dt,
X = x,
NU = 1,
METHOD = 1,
RANGE = Opt0$control$MAXT - Opt0$control$BURN,
TOTFLAG = TRUE,
PRINTFLAG = F
)
varList
varList <- sampleVar(
THETA = Opt0$theta,
DATA = dt,
X = x,
NU = 1,
METHOD = 2,
RANGE = Opt0$control$MAXT - Opt0$control$BURN,
TOTFLAG = TRUE,
PRINTFLAG = F
)
varList
library(gammaFrailty)
library(gammaFrailty)
varList <- sampleVar(
THETA = Opt0$theta,
DATA = dt,
X = x,
NU = 1,
METHOD = 2,
RANGE = Opt0$control$MAXT - Opt0$control$BURN,
TOTFLAG = TRUE,
PRINTFLAG = F
)
diag(varList$var$var_stoc*Opt0$control$MAXT/n)
varList
library(gammaFrailty)
varList <- sampleVar(
THETA = Opt0$theta,
DATA = dt,
X = x,
NU = 1,
METHOD = 2,
RANGE = Opt0$control$MAXT - Opt0$control$BURN,
TOTFLAG = TRUE,
PRINTFLAG = F
)
library(gammaFrailty)
varList <- sampleVar(
THETA = Opt0$theta,
DATA = dt,
X = x,
NU = 1,
METHOD = 2,
RANGE = Opt0$control$MAXT - Opt0$control$BURN,
TOTFLAG = TRUE,
PRINTFLAG = F
)
diag(varList$var$var_stoc*Opt0$control$MAXT/n)
varList
trajSub <- c(500, 1000, 2000)
trajSub <- c(trajSub[trajSub < maxT], maxT)
trajSub
trajSub <- c(50, 100, 1000, 2000)
trajSub <- c(trajSub[trajSub < maxT], maxT)
library(gammaFrailty)
maxT <- n; set.seed(1)
trajSub <- c(50, 100, 1000, 2000)
trajSub <- c(trajSub[trajSub < maxT], maxT)
system.time(
Opt <- gammaFrailty(
THETA_INIT = par_init,
DATA = dt,
X = X,
MAXT = maxT,
BURN = maxT*(2/3),
STEPSIZE = .01,
STEPSIZE0 = .001,
NU = 1,
METHODFLAG = 1,
VERBOSEFLAG = F,
STEPSIZEFLAG = F
)
)
system.time(
Opt <- gammaFrailty(
THETA_INIT = par_init,
DATA = dt,
X = X,
MAXT = maxT,
BURN = maxT*(2/3),
STEPSIZE = .01,
STEPSIZE0 = .001,
NU = 1,
METHODFLAG = 1,
VERBOSEFLAG = F,
STEPSIZEFLAG = F,
ITER_SUBSET = c(2,3,4)
)
)
system.time(
Opt <- gammaFrailty(
THETA_INIT = par_init,
DATA = dt,
X = X,
MAXT = maxT,
BURN = maxT*(2/3),
STEPSIZE = .01,
STEPSIZE0 = .001,
NU = 1,
METHODFLAG = 1,
VERBOSEFLAG = F,
STEPSIZEFLAG = F,
ITER_SUBSET = c(2,3,4)
)
)
library(gammaFrailty)
system.time(
Opt <- gammaFrailty(
THETA_INIT = par_init,
DATA = dt,
X = X,
MAXT = maxT,
BURN = maxT*(2/3),
STEPSIZE = .01,
STEPSIZE0 = .001,
NU = 1,
METHODFLAG = 1,
VERBOSEFLAG = F,
STEPSIZEFLAG = F,
ITER_SUBSET = c(2,3,4)
)
)
library(gammaFrailty)
library(gammaFrailty)
system.time(
Opt <- gammaFrailty(
THETA_INIT = par_init,
DATA = dt,
X = X,
MAXT = maxT,
BURN = maxT*(2/3),
STEPSIZE = .01,
STEPSIZE0 = .001,
NU = 1,
METHODFLAG = 1,
VERBOSEFLAG = F,
STEPSIZEFLAG = F,
ITER_SUBSET = c(2,3,4)
)
)
system.time(
Opt <- gammaFrailty(
THETA_INIT = par_init,
DATA = dt,
X = X,
MAXT = maxT,
BURN = maxT*(2/3),
STEPSIZE = .01,
STEPSIZE0 = .001,
NU = 1,
METHODFLAG = 1,
VERBOSEFLAG = F,
STEPSIZEFLAG = F,
ITER_SUBSET = c(2,3,4)
)
)
library(gammaFrailty)
Opt
system.time(
Opt <- gammaFrailty(
THETA_INIT = par_init,
DATA = dt,
X = X,
MAXT = maxT,
BURN = maxT*(2/3),
STEPSIZE = .01,
STEPSIZE0 = .001,
NU = 1,
METHODFLAG = 1,
VERBOSEFLAG = F,
STEPSIZEFLAG = F,
ITER_SUBSET = NULL
)
)
Opt
library(gammaFrailty)
trajSub <- c(50, 100, 1000, 2000)
trajSub <- c(trajSub[trajSub < maxT], maxT)
system.time(
Opt <- gammaFrailty(
THETA_INIT = par_init,
DATA = dt,
X = X,
MAXT = maxT,
BURN = maxT*(2/3),
STEPSIZE = .01,
STEPSIZE0 = .001,
NU = 1,
METHODFLAG = 1,
VERBOSEFLAG = F,
STEPSIZEFLAG = F,
ITER_SUBSET = NULL
)
)
trajSub <- c(50, 100, 1000, 2000)
trajSub <- c(trajSub[trajSub < maxT], maxT)
system.time(
Opt <- gammaFrailty(
THETA_INIT = par_init,
DATA = dt,
X = X,
MAXT = maxT,
BURN = maxT*(2/3),
STEPSIZE = .01,
STEPSIZE0 = .001,
NU = 1,
METHODFLAG = 1,
VERBOSEFLAG = F,
STEPSIZEFLAG = F,
ITER_SUBSET = NULL
)
)
Opt
system.time(
Opt <- gammaFrailty(
THETA_INIT = par_init,
DATA = dt,
X = X,
MAXT = maxT,
BURN = maxT*(2/3),
STEPSIZE = .01,
STEPSIZE0 = .001,
NU = 1,
METHODFLAG = 1,
VERBOSEFLAG = F,
STEPSIZEFLAG = F,
ITER_SUBSET = c(1,2,3,9)
)
)
Opt$path_theta[maxT,]
Opt
library(gammaFrailty)
library(gammaFrailty)
maxT <- n; set.seed(1)
trajSub <- c(50, 100, 1000, 2000)
trajSub <- c(trajSub[trajSub < maxT], maxT)
system.time(
Opt <- gammaFrailty(
THETA_INIT = par_init,
DATA = dt,
X = X,
MAXT = maxT,
BURN = maxT*(2/3),
STEPSIZE = .01,
STEPSIZE0 = .001,
NU = 1,
METHODFLAG = 1,
VERBOSEFLAG = F,
STEPSIZEFLAG = F,
ITER_SUBSET = c(1,2,3,9)
)
)
system.time(
Opt <- gammaFrailty(
THETA_INIT = par_init,
DATA = dt,
X = X,
MAXT = maxT,
BURN = maxT*(2/3),
STEPSIZE = .01,
STEPSIZE0 = .001,
NU = 1,
METHODFLAG = 1,
VERBOSEFLAG = F,
STEPSIZEFLAG = F,
ITER_SUBSET = NULL
)
)
system.time(
Opt <- gammaFrailty(
THETA_INIT = par_init,
DATA = dt,
X = X,
MAXT = maxT,
BURN = maxT*(2/3),
STEPSIZE = .01,
STEPSIZE0 = .001,
NU = 1,
METHODFLAG = 1,
VERBOSEFLAG = F,
STEPSIZEFLAG = F,
ITER_SUBSET = NULL
)
)
system.time(
Opt <- gammaFrailty(
THETA_INIT = par_init,
DATA = dt,
X = X,
MAXT = maxT,
BURN = maxT*(2/3),
STEPSIZE = .01,
STEPSIZE0 = .001,
NU = 1,
METHODFLAG = 1,
VERBOSEFLAG = F,
STEPSIZEFLAG = F,
ITER_SUBSET = NULL
)
)
library(gammaFrailty)
Opt
#Opt
mat_theta <- Opt$path_theta
mat_av_theta <- Opt$path_av_theta
trajSub <- c(50, 100, 1000, 2000)
trajSub <- c(trajSub[trajSub < maxT], maxT)
system.time(
Opt <- gammaFrailty(
THETA_INIT = par_init,
DATA = dt,
X = X,
MAXT = maxT,
BURN = maxT*(2/3),
STEPSIZE = .01,
STEPSIZE0 = .001,
NU = 1,
METHODFLAG = 1,
VERBOSEFLAG = F,
STEPSIZEFLAG = F,
ITER_SUBSET = trajSub
)
)
mat_theta[trajSub+1]
mat_theta[trajSub+1,]
Opt$path_theta
Opt$path_theta[1:length(trajSub)]
Opt$path_theta[1:length(trajSub),]
mat_theta[trajSub+1,]
Opt$path_theta[1:length(trajSub),]
trajSub
math_theta[51,]
mat_theta[51,]
Opt$path_theta[1:(length(trajSub)+1),]
Opt$path_theta[maxT,]
mat_theta[trajSub+1,]
Opt$path_theta[1:(length(trajSub)+1),]
mat_theta[51,]
library(gammaFrailty)
maxT <- n; set.seed(1)
trajSub <- c(50, 100, 1000, 2000)
trajSub <- c(trajSub[trajSub < maxT], maxT)
system.time(
Opt <- gammaFrailty(
THETA_INIT = par_init,
DATA = dt,
X = X,
MAXT = maxT,
BURN = maxT*(2/3),
STEPSIZE = .01,
STEPSIZE0 = .001,
NU = 1,
METHODFLAG = 1,
VERBOSEFLAG = F,
STEPSIZEFLAG = F,
ITER_SUBSET = trajSub
)
)
system.time(
Opt <- gammaFrailty(
THETA_INIT = par_init,
DATA = dt,
X = X,
MAXT = maxT,
BURN = maxT*(2/3),
STEPSIZE = .01,
STEPSIZE0 = .001,
NU = 1,
METHODFLAG = 1,
VERBOSEFLAG = F,
STEPSIZEFLAG = F,
ITER_SUBSET = NULL
)
)
#Opt
Opt$path_theta[c(51,101),]
#Opt
vec <- Opt$path_theta[c(51,101),]
system.time(
Opt <- gammaFrailty(
THETA_INIT = par_init,
DATA = dt,
X = X,
MAXT = maxT,
BURN = maxT*(2/3),
STEPSIZE = .01,
STEPSIZE0 = .001,
NU = 1,
METHODFLAG = 1,
VERBOSEFLAG = F,
STEPSIZEFLAG = F,
ITER_SUBSET = trajSub
)
)
Opt$path_theta[1:4,]
vec
library(gammaFrailty)
trajSub <- c(50, 100, 1000, 2000)
trajSub <- c(1, trajSub[trajSub < maxT]+1, maxT+1)
maxT <- n; set.seed(1)
trajSub <- c(50, 100, 1000, 2000)
trajSub <- c(1, trajSub[trajSub < maxT]+1, maxT+1)
trajSub
library(gammaFrailty)
library(gammaFrailty)
trajSub <- c(50, 100, 1000, 2000)
Opt0 <- fit_gammaFrailty(
DATA_LIST = list('DATA' = dt, 'X' = X),
METHOD = 'SGD',
CPP_CONTROL = ctrl,
VERBOSEFLAG= 0,
INIT = par_init
)
Opt0
Opt0 <- fit_gammaFrailty(
DATA_LIST = list('DATA' = dt, 'X' = X),
METHOD = 'SGD',
CPP_CONTROL = ctrl,
VERBOSEFLAG= 0,
INIT = par_init,
ITERATIONS_SUBSET = trajSub
)
library(gammaFrailty)
Opt0 <- fit_gammaFrailty(
DATA_LIST = list('DATA' = dt, 'X' = X),
METHOD = 'SGD',
CPP_CONTROL = ctrl,
VERBOSEFLAG= 0,
INIT = par_init,
ITERATIONS_SUBSET = trajSub
)
Opt0 <- fit_gammaFrailty(
DATA_LIST = list('DATA' = dt, 'X' = X),
METHOD = 'SGD',
CPP_CONTROL = ctrl,
VERBOSEFLAG= 0,
INIT = par_init,
ITERATIONS_SUBSET = trajSub
)
library(gammaFrailty)
Opt0
library(gammaFrailty)
library(gammaFrailty)
Opt0 <- fit_gammaFrailty(
DATA_LIST = list('DATA' = dt, 'X' = X),
METHOD = 'SGD',
CPP_CONTROL = ctrl,
VERBOSEFLAG= 0,
INIT = par_init,
ITERATIONS_SUBSET = trajSub
)
Opt0$time
Opt0
c(1, trajSub[trajSub < maxT]+1, maxT+1)
trajSub0 <- c(1, trajSub[trajSub < maxT]+1, maxT+1)
Opt1 <- fit_gammaFrailty(
DATA_LIST = list('DATA' = dt, 'X' = X),
METHOD = 'SGD',
CPP_CONTROL = ctrl,
VERBOSEFLAG= 0,
INIT = par_init,
ITERATIONS_SUBSET = NULL
)
Opt1$fit$path_theta[trajSub0,]
Opt0$fit$path_theta
library(gammaFrailty)
Opt1 <- fit_gammaFrailty(
DATA_LIST = list('DATA' = dt, 'X' = X),
METHOD = 'SGD',
CPP_CONTROL = ctrl,
VERBOSEFLAG= 0,
INIT = par_init,
ITERATIONS_SUBSET = NULL
)
Opt1$fit$path_theta[trajSub0,]
Opt0$fit$path_theta
Opt1$iterations_subset
trajSub <- c(50, 100, 1000, 2000)
Opt0 <- fit_gammaFrailty(
DATA_LIST = list('DATA' = dt, 'X' = X),
METHOD = 'SGD',
CPP_CONTROL = ctrl,
VERBOSEFLAG= 0,
INIT = par_init,
ITERATIONS_SUBSET = trajSub
)
Opt0$iterations_subset
par_init
repar_theta
